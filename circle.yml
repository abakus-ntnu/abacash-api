machine:
  node:
    version: 6.7.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - npm install -g yarn
  override:
    - yarn

test:
  pre:
    - sudo -u postgres createdb abacash-test
  override:
    - yarn test -- --timeout 0
    - yarn run lint
